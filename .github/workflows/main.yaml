name: Testes qualitativos

on:
  push:
    branches: [ "actions" ]

jobs:
  padrao-de-codigo:
    runs-on: ubuntu-latest

    steps:
    - uses: shivammathur/setup-php@v2
      with:
        php-version: '8.1'
    - uses: actions/checkout@v3

    - name: Definir arquivo de ambiente
      run: php -r "file_exists('.env') || copy('.env.example', '.env');"

    - name: Instalar Dependências
      run: composer require squizlabs/php_codesniffer --working-dir tests -q --no-interaction --no-progress

    - name: Verificar padrão de código
      run: |
        ./tests/vendor/bin/phpcs --standard=PSR12 ./app
        ./tests/vendor/bin/phpcs --standard=PSR12 ./routes

  testes:
    runs-on: ubuntu-latest
    env:
      DB_HOST: localhost
      DB_CONNECTION: pgsql
      DB_PASSWORD: postgres
      DB_USERNAME: postgres
      DB_DATABASE: postgres
    
    services:
      postgres:
        image: postgres:14
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready 
          --health-interval 10s 
          --health-timeout 5s 
          --health-retries 3

    steps:
    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.1'
        extensions: mbstring, fileinfo, pgsql
    
    - name: Checkout
      uses: actions/checkout@v3

    - name: Definir arquivo de ambiente
      run: php -r "file_exists('.env') || copy('.env.example', '.env');"
    
    - name: Instalar Dependências
      run: |
        composer install -q --no-interaction --no-progress
        php artisan key:generate

    - name: Limpar cache/config
      run: php artisan config:clear

    - name: Executar migrations
      env:
        DB_PORT: 5432
      run: php artisan migrate -v --seed

    - name: Realizar testes
      env:
        DB_PORT: 5432
        APP_URL: localhost:8080
      run: php artisan test
